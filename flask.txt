secure_app/
│
├── app.py              # Uygulamanın ana giriş noktası (Flask config)
├── models.py           # Veritabanı modelleri (User, Role)
├── auth.py             # Kimlik doğrulama mantığı (Login/Register)
├── crypto_utils.py     # Şifreleme ve Hash algoritmaları (Fernet/Bcrypt)
├── templates/          # HTML dosyaları (login, dashboard, admin_panel)
├── instance/           # SQLite veritabanı dosyası
└── requirements.txt    # Gerekli kütüphaneler (flask, flask-sqlalchemy, bcrypt, cryptography)